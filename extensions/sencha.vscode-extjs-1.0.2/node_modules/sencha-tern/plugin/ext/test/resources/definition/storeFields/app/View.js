Ext.define('Test.view.View', {
    viewModel: 'user',

    items: [{
        xtype: 'gridpanel',
        bind: {
            store: '{users}'
        },
        columns: [{
            dataIndex: 'name/* caret */'
        }]
    }]
});