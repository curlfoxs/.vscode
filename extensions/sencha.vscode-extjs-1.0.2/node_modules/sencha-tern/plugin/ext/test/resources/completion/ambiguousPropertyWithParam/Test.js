/**
 * As seen in Ext.dom.Element#getMargin
 */
Ext.define('Test', function() {
    return {
        /**
         * @param {String} someParam
         */
        methodWithParam: (function() {
            return function() {

            }
        }),

        /**
         * @return {String} some value
         */
        methodWithReturn: (function() {
            return function() {

            }
        }),

        setId: function(id) {
            var me = this,
                currentId = me.id,
                cache = Ext.cache;

            if (currentId) {
                delete cache[currentId];
            }

            me.dom.id = id;

            /**
             * The DOM element ID
             * @property id
             * @type String
             */
            me.id = id;

            cache[id] = me;

            return me;
        }
    }
});

new Test()./* caret */;
Ext.create('Test')./* caret */;