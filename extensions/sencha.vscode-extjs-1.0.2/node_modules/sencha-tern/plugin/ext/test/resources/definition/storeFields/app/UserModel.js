Ext.define('Test.view.UserModel', {
    alias: 'viewmodel.user',

    stores: {
        users: {
            fields: [
                /* origin */{ name: 'name' },
                { name: 'email' }
            ]
        }
    }
});