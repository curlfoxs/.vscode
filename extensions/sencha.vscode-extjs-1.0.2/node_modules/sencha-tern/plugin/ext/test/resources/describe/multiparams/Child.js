Ext.define('Child', {
    /**
     * @param {String} p1
     * @param {Boolean} p2
     * @param {Function} p3
     * @param {Object} p4
     * @param {Number} p5
     * @param {Symbol} p6
     * @param {RegExp} p7
     * @param {Array} p8
     * @return {Function} r1
     *
     */
    testMethod: function(x,y,z,o,n,s,r,a) {
        return fn();
    }

});

Ext.create('Child').testMethod(x,y,z,o,n,s,r/* caret */,a)


