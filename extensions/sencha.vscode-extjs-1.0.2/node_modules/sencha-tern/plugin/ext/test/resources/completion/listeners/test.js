(function() {
    Ext.create('View', {
        listeners: {
            /* caret */
        }
    })

    var view = new View({
        listeners: {
            /* caret */
        }
    });

    view.on('/* caret */');
    view.mon('/* caret */');
    view.un('/* caret */');
    view.mun('/* caret */');

    view.on(/* unquoted */);
    view.mon(/* unquoted */);
    view.un(/* unquoted */);
    view.mun(/* unquoted */);
})();
