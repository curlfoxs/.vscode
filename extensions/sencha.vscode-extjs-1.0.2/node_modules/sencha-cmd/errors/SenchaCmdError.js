'use strict';

class SenchaCmdError extends Error {
    constructor(cmd) {
        super(cmd.errors.length > 0 ? cmd.errors.join('\n') : 'Cmd Error');
        this.details = cmd.messages;
    }
}

module.exports = SenchaCmdError;