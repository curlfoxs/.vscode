const SenchaCmd = require('../../index');

module.exports = {
	stopProcesses () {
		var cmdProcesses = SenchaCmd.getProcesses();
	    let promise;
	    for(let pid in cmdProcesses) {
	        cmdProcesses[pid].stop();
	        console.log('process ', pid, ' stopped!')
	    }
	}	
}
